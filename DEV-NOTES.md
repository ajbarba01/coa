
- scroll bar fix
- no rounding edges for panels on edges of windows. they should just go until the edge
- text input max chars
- keyboard shortcuts
- collapsable nav rail panel
- proper toggleable sidebar for agents sessions and subagent sessions. grouped by local, project, and pinned. each agent has list of sessions underneath, new session button, spinner if currently running, and sessions can have nested subagent sessions. idk honestly
- agents searchable by model and model shows full model
- default claude agent in chat model select
- staleness banner on fresh session
- always visible todowrite
- audio record button
- dropdowns can overflow bottom of window. (no component should be able to do this ofc)
- seam between nav rail and left window
- copy block toast
- chat inline "switched model" message
- is that how toasts are supposed to be activated lol.
- thinking highlight color
- user message shadow and proper push and expand 
- larger "working" indicator. same size as other text.
- no scroll bar in message the agent unless message long enough
- holy shit whys everything so buggy and jittery
- restarting daemon should work and also whenever the daemon is stopped it shouldn't say "error" but "off" and should also make the whole window just a simple daemon not running screen.
- claude.md or agents.md injection?
- switching chat mid running causes the output to be piped to the chat you switched to (lmao its cooked)
- vertical gap in spine


### CHAT
- file references (@)
- file uploads
- permission mode
- stop
- voice input
- copy button in code block
- auto compressing your input text (maybe?? at least get rid of like "ok so" type shit) or like compressing things that can be compressed (deferred)
- ctrl f search
